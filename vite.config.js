
// Importamos defineConfig para definir configuraciones de Vite
import { defineConfig } from 'vite';



/**
 * Aqui se pueden añadir plugins
 * Aqui se puede canbiar cual va ser el directorio publico
 * Aqui se puede hacer cualquier otro cambio
 */
export default defineConfig({
    // Con base colocamos el repositorio root que sale en github
    base: '/js-vite-blackjack/',
    plugins: []
});